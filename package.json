{
  "name": "workflow-functions",
  "version": "1.0.0",
  "description": "Nhost serverless functions for AI Agent Workflow Builder",
  "scripts": {
    "build": "echo 'No build step required'",
    "dev": "ts-node-dev --respawn --transpile-only triggerWorkflowRun.ts"
  },
  "dependencies": {
    "@types/express": "^4.17.21",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  }
}
