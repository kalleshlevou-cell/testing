# Postgres
POSTGRES_PASSWORD=secretpgpassword

# Hasura
HASURA_GRAPHQL_ADMIN_SECRET=nhost-admin-secret
NHOST_JWT_SECRET=0f987876650b4a085e64594fae9219e7762aec5e8070e0ca6e0a3ef94d2d2370

# Webhook secret (shared between Hasura event triggers and function validation)
WEBHOOK_SECRET=local-webhook-secret
NHOST_WEBHOOK_SECRET=local-webhook-secret

# LLM API keys (pick one — app stubs if neither is set)
GROQ_API_KEY=
OPENROUTER_API_KEY=
OPENAI_API_KEY=

# Auth redirect (for email verification flows)
AUTH_CLIENT_URL=http://localhost:3000
